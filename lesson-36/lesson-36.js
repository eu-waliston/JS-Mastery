//Type Coercion

// reference -> https://developer.mozilla.org/en-US/docs/Glossary/Type_coercion

/*
It's mean that JS will change the value automatically for us, if  the JS think that something its a number it will be a number an so on
*/

console.log(0 == false);
console.log(0 == true);